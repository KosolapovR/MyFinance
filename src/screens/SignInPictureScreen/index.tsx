import React from 'react';
import {View, Text} from 'react-native';

function SignInPictureScreen() {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
      }}>
      <Text>SignInPictureScreen</Text>
    </View>
  );
}

export default SignInPictureScreen;
